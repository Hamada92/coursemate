%h3
  =@university.name
%h4 Tags
-@courses.each do |c|
  =link_to("#{c.name}", show_questions_in_course_path(c), class: "btn btn-xs btn-info", role: "button")
%h4 Questions

=render @questions
=will_paginate @questions, renderer: BootstrapPagination::Rails
