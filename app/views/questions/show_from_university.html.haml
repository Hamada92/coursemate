%h3
  =@university
%h4 Tags
-@tags.each do |t|
  =link_to("#{t.name}", show_with_tag_path(t.id, t.name.gsub(" ", "-")), class: "btn btn-xs btn-info")
%h4 Questions
-@questions_from_university.each do |q|
  =render 'questions/link', question: q
