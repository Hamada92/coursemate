%p
  =link_to question.title, question_path(question)
  %br
  =question.body
  %br
  tags:
  =link_to("#{question.tags.first.university}", show_with_university_tag_path(question.tags.first.university))
  =link_to("#{question.tags.first.name}", show_with_tag_path(question.tags.first.id))