%h2 Universities
-@universities.each do |u|
  =link_to("#{u}", show_with_university_tag_path(u), class: "btn btn-xs btn-info")
%h2 Questions
-@questions.each do |q|
  =render 'questions/link', question: q