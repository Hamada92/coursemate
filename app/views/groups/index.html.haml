%h4 Universities
.container
  -@universities.each_slice(6) do |slice|
    .row
      -slice.each do |u|
        .col-md-2
          =link_to("#{u.name}", show_groups_in_university_path(u.id) , class: "btn btn-xs btn-info", role: "button")

%h4 Groups

=render @groups
=will_paginate @groups, renderer: BootstrapPagination::Rails