.group-show
  = render 'groups/group_card'
  
  %h3
    Students attending: 
    .frow.attendees
      -@attendees.each do |u|
        = render 'group_enrollments/attendee', u: u
    .available-seats
    Available Seats: 
    %span.seats
      = @group.available_seats
 
  - if user_signed_in?
    = render 'group_enrollments/enrollment'
  - else
    .login-to-join
      Sign in to attend this group.
