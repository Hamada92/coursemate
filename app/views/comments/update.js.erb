$("#Comment<%=@comment.id%> .Errors").html("<%= j (render 'partials/errors', resource: @comment) %>");
<% unless @comment.errors.any? %>
  $("#Comment<%=@comment.id%>").html("<%= j (render @comment) %>");
<%end%>