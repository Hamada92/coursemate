$("#<%=@comment.commentable_type%><%=@commentable.id%> .NewComment .Errors").html("<%= j (render 'partials/errors', resource: @comment) %>");
if ("<%=@comment.errors.any?%>" == "false") {
  $("#<%=@comment.commentable_type%><%=@commentable.id%> .ExistingComments").append("<%= j (render @comment) %>");
  $("#<%=@comment.commentable_type%><%=@commentable.id%> .NewComment form").trigger("reset");
}
