-if resource.errors.any?
  .alert.alert-danger.alert-dismissible{role: "alert"}
    %button.close{type: "button", "data-dismiss" => "alert"}
      %span &times;
    %h4="#{pluralize(resource.errors.count, "error")}:"
    -resource.errors.full_messages.each do |msg|
      %li= msg

